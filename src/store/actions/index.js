import * as currentLoggedUser from './currentLoggedUser.actions';
import * as kakeleActions from './kakeleCurrentSet.actions';
import * as kakeleFilterItemsActions from './KakeleFilters.actions';

const actions = {
  currentLoggedUser,
  kakeleActions,
  kakeleFilterItemsActions,
};

export default actions;
